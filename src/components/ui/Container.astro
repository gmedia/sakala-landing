---
export interface Props {
  className?: string;
  as?: "div" | "section" | "nav";
}

const { className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-6xl px-6 lg:px-8", className]}>
  <slot />
</Tag>
